# Public form submissions are routed directly to the Bag Supply Co webhook endpoints.

# Required only to regenerate data/seo/cities.json via `node scripts/seo/build-seo-data.mjs`.
# Free, instant signup: https://api.census.gov/data/key_signup.html (must click the activation
# link in the confirmation email before the key works).
CENSUS_API_KEY=
